---
phase: 06-phat-hanh
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - supabase/migrations/0021_order_parent.sql
  - lib/orders/order-group.ts
  - lib/orders/takeaway-group.ts
  - lib/orders/create-order.ts
  - lib/orders/online.ts
  - lib/billing/bill.ts
  - app/r/[slug]/pos/actions.ts
  - components/pos/TakeawayPanel.tsx
  - components/pos/PosBoard.tsx
  - tests/orders/takeaway-group.test.ts
autonomous: false
requirements: [ORDER-14]
user_setup:
  - "Áp migration 0021 vào Supabase dev."
  - "Tenant có ≥1 món đang bán; đăng nhập /pos bằng owner (mật khẩu) hoặc nhân viên (email+PIN)."
must_haves:
  truths:
    - "Nút 'Gọi thêm' trên đơn đang chờ → builder sang chế độ nối; đơn mới có parent_order_id trỏ đơn GỐC"
    - "Lượt gọi thêm là ĐƠN THẬT: kitchen_no riêng, phiếu bếp riêng, vé KDS riêng"
    - "Panel hiện nhóm thành MỘT khối, tổng gộp, MỘT nút 'Thu tiền & hoàn tất'"
    - "openBillForOrder gom order_items CẢ NHÓM và ĐỒNG BỘ LẠI bill đang mở khi có món mới"
    - "payBill hoàn tất MỌI đơn trong nhóm, không chỉ đơn neo bill"
    - "Hủy đơn gốc = hủy cả nhóm (1 lý do, 1 lần duyệt PIN); hủy đơn con chỉ đơn đó"
  artifacts:
    - "0021_order_parent.sql: orders.parent_order_id + index (tenant_id, parent_order_id)"
    - "lib/orders/order-group.ts (server): resolveGroupRoot / groupOrderIds / groupIsPaid"
    - "lib/orders/takeaway-group.ts (THUẦN, dùng được ở client): groupTakeawayOrders"
    - "TakeawayPanel: chế độ 'Đang thêm vào Đơn #N' + khối nhóm + 2 cột cuộn riêng"
  key_links:
    - "Bill neo vào GỐC qua cột SẴN CÓ bills.online_order_id — không thêm cột bill nào"
    - "payBill đã roll-up mọi order trong bill sang 'served'; chỉ cần mở rộng bước 'completed' cho cả nhóm"
    - "online.ts có `import server-only` → hàm gom nhóm PHẢI nằm ở file riêng thì client mới import được"
    - "memberships/orders RLS chỉ cách ly tenant_id → không cần policy mới"
---

<objective>
Cho khách mua mang về / ăn tại quầy gọi thêm món mà vẫn chỉ trả tiền MỘT lần: mỗi lượt gọi thêm là một đơn thật (bếp làm theo lượt) nhưng được nối vào đơn gốc NGAY LÚC TẠO, và cả nhóm thu chung một hóa đơn. Đóng ORDER-14.

Purpose: Gỡ tình trạng khách gọi thêm → nhân viên buộc phải tạo đơn rời → khách trả tiền 2 lần, cầm 2 hóa đơn; hoặc phải hủy đơn cũ đã xuống bếp để gõ lại.
Output: migration 0021, nhóm đơn ở backend + bill gộp nhóm, UI "Gọi thêm" + khối nhóm, test.
</objective>

<context>
@docs/15-QuyetDinh/QD-011-GoiThemChoDonKhongBan.md  (5 quyết định — nguồn sự thật)
@docs/15-QuyetDinh/QD-008-KenhOnlineP5.md  (§1 "1 đơn = 1 bill" — nay nới thành "1 NHÓM = 1 bill")
@lib/billing/bill.ts  (openBillForOrder + payBill)
@lib/orders/create-order.ts  (insertOrderGraph + createStaffTakeawayOrder)
@lib/orders/online.ts  (OnlineOrderView + listTakeawayOrders)
@components/pos/TakeawayPanel.tsx  (panel đơn không bàn)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + tiện ích nhóm</name>
  <files>supabase/migrations/0021_order_parent.sql, lib/orders/order-group.ts, lib/orders/takeaway-group.ts, tests/orders/takeaway-group.test.ts</files>
  <action>
  0021: `orders.parent_order_id uuid references orders(id) on delete set null` + index partial `(tenant_id, parent_order_id) where parent_order_id is not null`. Không đổi RLS.
  order-group.ts (server-only): `resolveGroupRoot` (chuẩn hóa về gốc — bấm "Gọi thêm" trên đơn con vẫn trỏ gốc; chặn kênh dine_in, chặn đơn đã kết thúc), `groupOrderIds`, `groupIsPaid`.
  takeaway-group.ts (THUẦN — không server-only): `groupTakeawayOrders(orders)` gom theo parentOrderId, giữ thứ tự đơn gốc, **nâng đơn con mồ côi thành nhóm riêng** (gốc rời hàng đợi thì con vẫn phải thu được tiền).
  Test: nhóm rời rạc, gốc+2 con, thứ tự gốc, con mồ côi, rỗng, tổng = Σ.
  </action>
  <verify>vitest tests/orders xanh; tsc sạch</verify>
  <acceptance_criteria>
    - Nhóm phẳng 1 tầng; con mồ côi không biến mất
    - Hàm gom nhóm import được từ client component
  </acceptance_criteria>
  <done>Nền tảng nhóm đơn</done>
</task>

<task type="auto">
  <name>Task 2: Tạo đơn con + bill gộp nhóm + hoàn tất cả nhóm</name>
  <files>lib/orders/create-order.ts, lib/orders/online.ts, lib/billing/bill.ts, app/r/[slug]/pos/actions.ts</files>
  <action>
  create-order.ts: `insertOrderGraph` + `createStaffTakeawayOrder` nhận `parentOrderId` (ghi thẳng vào cột).
  online.ts: `OnlineOrderView` thêm `parentOrderId`; SELECT thêm cột.
  bill.ts `openBillForOrder`: neo bill vào GỐC; gom order_items của cả nhóm; nếu đã có bill `open` thì **đồng bộ lại** (`syncGroupBillItems`: thêm món mới, bỏ món đã hủy) rồi `recomputeBill`; bill `paid` giữ nguyên.
  bill.ts `payBill`: nhánh `online_order_id` nâng **mọi đơn trong nhóm** sang `completed` + broadcast từng đơn.
  pos/actions.ts `createTakeawayOrderAction(..., addToOrderId?)`: chuẩn hóa gốc bằng `resolveGroupRoot` Ở SERVER (client không tự quyết cha) + chặn khi `groupIsPaid`.
  pos/actions.ts `cancelOrder`: nếu đích là gốc nhóm (không dine_in, parent null) thì gom `groupOrderIds` và hủy cả nhóm với cùng lý do; giữ nguyên chặn "có món đã phục vụ".
  </action>
  <verify>Tạo nhóm 2 đơn → 1 bill, tổng = Σ; thu tiền → cả 2 đơn completed; thêm món sau khi mở bill → tổng bill tự cập nhật</verify>
  <acceptance_criteria>
    - 1 nhóm = 1 bill = 1 lần thu; doanh thu không đếm trùng
    - Bill đang mở tự đồng bộ khi nhóm có món mới
  </acceptance_criteria>
  <done>ORDER-14 phần nghiệp vụ</done>
</task>

<task type="auto">
  <name>Task 3: UI panel đơn không bàn</name>
  <files>components/pos/TakeawayPanel.tsx, components/pos/PosBoard.tsx</files>
  <design>
  BÁM QD-006. Panel ~26rem không đủ cho 4 nút một hàng → header giữ ĐÚNG 3 điều khiển (Phiếu bếp / Phiếu khách / Hủy đơn); "Gọi thêm" là nút hàng riêng dưới danh sách món; số lượt gọi thêm hiện ở TỔNG. Thông tin khách (tên + SĐT) chiếm HÀNG RIÊNG full-width.
  HAI CỘT (xl+): gõ đơn mới bên trái, hàng đợi bên phải, mỗi cột cuộn riêng — quán đông thì ô gõ đơn không bị đẩy khuất.
  </design>
  <action>
  TakeawayPanel: gom `groups`; khối nhóm = đơn gốc + các lượt gọi thêm + tổng gộp + MỘT nút thu tiền; nút "Gọi thêm" đặt `addToOrderId`; builder đổi sang băng "Đang thêm vào Đơn #N" + "Bỏ liên kết", ẩn ô tên/SĐT (dùng lại của gốc), nhãn nút thành "Gửi bếp lượt gọi thêm". Tự thoát chế độ nếu nhóm biến mất.
  Ô "Tìm số đơn": chế độ quầy chuyển hẳn vào header panel (không có bàn để tìm); chọn kết quả → **LỌC** hàng đợi còn đúng nhóm đó + chip "Đang lọc: Đơn #N ✕". KHÔNG dùng `enterTakeaway()` (xóa giỏ đang gõ).
  PosBoard: nới bề ngang aside khi ở chế độ đơn không bàn để đủ 2 cột.
  </action>
  <verify>Xem checkpoint</verify>
  <acceptance_criteria>
    - Nhóm hiện 1 khối, 1 nút thu tiền; tiêu đề không vỡ; khách hiện đủ tên + SĐT
    - Tìm số đơn lọc được và bỏ lọc được
  </acceptance_criteria>
  <done>ORDER-14 phần giao diện</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Gọi thêm cho đơn mang về / tại quầy: 1 nhóm = 1 hóa đơn = 1 lần thu. Dev localhost:3000.</what-built>
  <how-to-verify>
    1. /pos → gõ 1 món → "Tạo đơn" (điền tên + SĐT khách) → đơn vào hàng đợi, hiện đủ tên và SĐT.
    2. Bấm "Gọi thêm" trên đơn đó → ô gõ đơn đổi thành "Đang thêm vào Đơn #N", KHÔNG hỏi lại tên/SĐT.
    3. Gõ thêm 1 món → "Gửi bếp lượt gọi thêm" → khối đơn hiện thêm mục "↳ Lượt #M", tổng cộng dồn, vẫn CHỈ MỘT nút "Thu tiền & hoàn tất".
    4. In phiếu bếp cho lượt gọi thêm → phiếu chỉ có món của lượt đó (bếp không làm trùng món cũ).
    5. KDS: lượt gọi thêm hiện thành vé riêng.
    6. "Thu tiền & hoàn tất" → hóa đơn gồm CẢ hai lượt, tổng đúng → thu → cả nhóm rời hàng đợi.
    7. /admin/reports → doanh thu tăng đúng MỘT bill bằng tổng nhóm (không đếm trùng).
    8. Tạo nhóm mới, bấm "Thu tiền" (mở bill) rồi ĐÓNG dialog, gọi thêm 1 món nữa, mở lại → tổng bill ĐÃ cập nhật.
    9. "Hủy đơn" trên đơn gốc có lượt gọi thêm → hủy cả nhóm sau 1 lý do; "Hủy lượt" chỉ hủy lượt đó.
    10. Ô "Tìm số đơn" (header panel) → gõ số → chọn → hàng đợi lọc còn đúng nhóm; bấm chip để bỏ lọc.
    11. Màn ≥1280px: hai cột nằm cạnh nhau, mỗi cột cuộn riêng.
    12. (Owner Bún Bò) không thấy đơn Phở Việt.
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (tổng sai, bếp in trùng, nhóm không rời hàng đợi, lọc không đúng).</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npx vitest run` xanh (gồm takeaway-group + RLS)
- [ ] `npx tsc --noEmit` + `npm run lint` + `npm run build` sạch
- [ ] 1 nhóm = 1 bill = 1 lần thu; cả nhóm `completed` (ORDER-14)
- [ ] Bill đang mở tự đồng bộ khi gọi thêm
- [ ] Không hồi quy: đơn online khách tự đặt (1 đơn, không con) vẫn thu tiền bình thường
</verification>

<success_criteria>
- Checkpoint "approved"
- ORDER-14 đóng; QD-008 §1 được ghi nhận là đã nới sang "1 NHÓM đơn = 1 bill"
</success_criteria>

<output>
Tạo `docs/30-KeHoach/P6/06-02-SUMMARY.md`. Cập nhật `20-DanhSachYeuCau/00-Requirements.md` (ORDER-14), `00-TongQuan/Roadmap.md` (P6), `00-TongQuan/BanDoLienKet.md` (ghi chú luồng), `15-QuyetDinh/QD-008` (trỏ sang QD-011).
</output>

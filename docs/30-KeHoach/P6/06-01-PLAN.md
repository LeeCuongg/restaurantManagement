---
phase: 06-phat-hanh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/auth/rbac.ts
  - components/admin/AdminShell.tsx
  - components/admin/AdminNav.tsx
  - app/r/[slug]/admin/(protected)/staff/page.tsx
  - app/r/[slug]/admin/(protected)/staff/actions.ts
  - app/r/[slug]/admin/(protected)/menu/actions.ts
  - components/menu/AvailabilityToggle.tsx
  - components/pos/MenuPanel.tsx
  - components/kds/KdsBoard.tsx
  - components/kds/SoldOutDrawer.tsx
  - app/r/[slug]/kds/page.tsx
  - tests/auth/rbac.test.ts
autonomous: false
requirements: [AUTH-05, AUTH-06, MENU-04]
user_setup:
  - "Có tenant demo (`pho-viet`) với ≥1 danh mục + ≥3 món (P2/onboarding) và ≥1 nhân viên cashier + 1 kitchen (seed hoặc /admin/staff)."
  - "Cần 1 tài khoản vai trò `manager` để kiểm §AUTH-05. Chưa có thì Task 3 tạo được — nên chạy Task 3 TRƯỚC khi kiểm checkpoint mục 2."
must_haves:
  truths:
    - "canManage(role, section) là ma trận thật: settings CHỈ owner; menu/tables/staff/onboarding/reports cho owner+manager (QD-010 §2)"
    - "Manager đăng nhập → sidebar KHÔNG có 'Cài đặt'; gõ thẳng /admin/settings → redirect về /admin (không hiện trang rồi mới chặn)"
    - "Owner tạo được thành viên role=manager ở /admin/staff bằng MẬT KHẨU ≥8 ký tự (không phải PIN 4 số); manager KHÔNG tạo được manager"
    - "Nhân viên POS bật/tắt 'hết món' ngay trên MenuPanel; bếp bật/tắt qua drawer 'Báo hết món' ở KDS — không ai phải vào /admin"
    - "canToggleAvailability chỉ mở đúng cột is_available: cashier gọi được setItemAvailable nhưng updateItem/deleteItem vẫn từ chối"
  artifacts:
    - "lib/auth/rbac.ts: canManage dùng THẬT tham số section (bỏ dấu _) + canToggleAvailability + canAssignRole"
    - "AdminNav nhận role, lọc items bằng chính canManage — không có bảng quyền thứ hai chép tay"
    - "staff/actions.ts: nhánh manager (password) vs nhánh trạm (PIN); guard canAssignRole ở SERVER, không chỉ ẩn option ở form"
    - "components/kds/SoldOutDrawer.tsx: danh sách món + ô tìm + AvailabilityToggle, dùng lại getCustomerMenu"
    - "tests/auth/rbac.test.ts: 6 vai trò × 6 mục + canToggleAvailability + canAssignRole"
  key_links:
    - "canManage hiện có chữ ký ĐÚNG rồi (role, section) nhưng bỏ qua section (rbac.ts:65-67) — chỉ cần thay thân hàm, KHÔNG đổi call-site nào trong 8 file đang gọi"
    - "AdminShell đã nhận role sẵn (hiện nhãn vai trò) → chỉ truyền tiếp xuống AdminNav"
    - "getCustomerMenu TRẢ CẢ món is_available=false (customer-menu.ts:63) — MenuPanel tự lọc ở dòng 52; bỏ lọc đó là đủ dữ liệu, không đụng query"
    - "setItemAvailable (menu/actions.ts:339) đang dùng chung guard canManage('menu') với updateItem/deleteItem — tách riêng nó ra canToggleAvailability, các action khác GIỮ NGUYÊN"
    - "memberships.role đã có 'manager' trong CHECK (0001_core_tenant.sql:37) → KHÔNG cần migration"
---

<objective>
Biến RBAC khu admin từ nhị phân (owner=manager) thành ma trận theo mục, cấp cho owner đường tạo tài khoản `manager` từ UI, và đưa thao tác "hết món" xuống đúng chỗ vận hành (POS/KDS). Đóng AUTH-05, AUTH-06, MENU-04.

Purpose: Gỡ thế kẹt "nhân viên cần phụ setup thực đơn ⇒ phải nâng lên manager ⇒ thấy luôn doanh thu + sửa được %phí/%VAT", đồng thời đóng lỗ hổng manager tự nhân bản quyền.
Output: ma trận `canManage`, sidebar theo quyền, nhánh tạo manager có mật khẩu mạnh, toggle "hết món" ở POS + KDS, test phân quyền.
</objective>

<context>
@docs/15-QuyetDinh/QD-010-PhanQuyenKhuAdmin.md  (5 quyết định — nguồn sự thật của plan này)
@docs/15-QuyetDinh/QD-009-DangNhapNhanVienEmailPin.md  (vì sao PIN 4 số chỉ hợp cho vai trò trạm)
@lib/auth/rbac.ts  (canAccess / canManage / canManageStaff — file trung tâm)
@lib/auth/session.ts  (Role union, getSessionMembership)
@components/admin/AdminShell.tsx  (đã có role, truyền xuống nav)
@components/admin/AdminNav.tsx  (items hardcode — cần lọc)
@app/r/[slug]/admin/(protected)/staff/actions.ts  (PIN_ROLES, createStaff, requireManager)
@app/r/[slug]/admin/(protected)/menu/actions.ts  (requireManage + setItemAvailable dòng 339)
@components/menu/AvailabilityToggle.tsx  (switch optimistic — dùng lại nguyên vẹn)
@components/pos/MenuPanel.tsx  (dòng 39 + 52: chỗ đang lọc/chặn món hết)
@lib/orders/customer-menu.ts  (getCustomerMenu — đã trả cả món hết)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ma trận canManage + quyền mới + sidebar theo quyền</name>
  <files>lib/auth/rbac.ts, components/admin/AdminShell.tsx, components/admin/AdminNav.tsx, tests/auth/rbac.test.ts</files>
  <read_first>lib/auth/rbac.ts (toàn bộ), lib/auth/session.ts (Role), components/admin/AdminShell.tsx, components/admin/AdminNav.tsx</read_first>
  <action>
  rbac.ts:
  (a) `ManageSection` thêm `"staff"` (hiện thiếu, đang xử lý riêng bằng `canManageStaff`).
  (b) Thay thân `canManage(role, section)` bằng ma trận QD-010 §2 — dùng THẬT tham số `section`, bỏ dấu `_`: `settings` → chỉ `owner`; `menu`/`tables`/`staff`/`onboarding`/`reports` → `owner|manager`. Viết dạng `switch (section)` để thêm mục mới bắt buộc phải khai quyền (exhaustive check của TS bắt sót).
  (c) `canManageStaff(role)` giữ nguyên hành vi, đổi thành `canManage(role, "staff")` để một nguồn sự thật.
  (d) Thêm `canToggleAvailability(role): boolean` → owner|manager|cashier|waiter|kitchen|station (mọi vai trò có phiên nội bộ).
  (e) Thêm `canAssignRole(actorRole, targetRole): boolean` → owner gán được `manager|cashier|waiter|kitchen`; manager CHỈ gán được `cashier|waiter|kitchen`; vai trò khác false. KHÔNG ai gán được `owner`/`station` qua UI.
  KHÔNG đổi `canAccess` (QD-010 §1: khu admin vẫn owner|manager).

  AdminShell.tsx: truyền `role` xuống `<AdminNav base={base} role={role} />`.
  AdminNav.tsx: nhận `role: Role`, gắn `section?: ManageSection` cho từng item, lọc `items.filter(i => !i.section || canManage(role, i.section))`. Mục "Tổng quan" (`dashboard`) không có `section` → luôn hiện. Import `canManage` từ rbac (hàm thuần, dùng được trong client component).

  tests/auth/rbac.test.ts (vitest, không cần DB): bảng 6 vai trò × 6 mục khẳng định đúng QD-010 §2; `canAccess` không đổi (kitchen/cashier/waiter/station → admin = false); `canToggleAvailability` đúng 6 vai trò; `canAssignRole` — owner+manager→true, manager+manager→false, manager+owner→false, cashier+cashier→false.
  </action>
  <verify>npx vitest run tests/auth/rbac.test.ts xanh; npx tsc --noEmit sạch (8 call-site canManage hiện có không phải sửa vì chữ ký không đổi)</verify>
  <acceptance_criteria>
    - canManage phân biệt được section; settings chỉ owner
    - AdminNav ẩn mục không có quyền, lấy quyền TỪ canManage (không chép tay danh sách vai trò)
    - Test 6×6 + canToggleAvailability + canAssignRole xanh
  </acceptance_criteria>
  <done>AUTH-05 phần logic + điều hướng</done>
</task>

<task type="auto">
  <name>Task 2: Guard trang settings + rà toàn bộ trang admin</name>
  <files>app/r/[slug]/admin/(protected)/settings/page.tsx, app/r/[slug]/admin/(protected)/settings/actions.ts, app/r/[slug]/admin/(protected)/staff/page.tsx</files>
  <read_first>app/r/[slug]/admin/(protected)/settings/page.tsx, settings/actions.ts, staff/page.tsx, admin/(protected)/layout.tsx</read_first>
  <action>
  settings/page.tsx + settings/actions.ts đã gọi `canManage(session.role,'settings')` — sau Task 1 chúng TỰ chặn manager, không cần sửa. Việc của task này là **xác minh + bịt chỗ hở**:
  (a) Chạy soát: mọi `page.tsx` và mọi server action trong `admin/(protected)/**` đều phải có guard `canManage` với đúng section của nó. Hiện `staff/page.tsx` KHÔNG có guard cấp trang (chỉ dựa layout `canAccess`) → thêm `if (!canManage(session.role,'staff')) redirect(defaultRouteForRole(slug, session.role))` cho đồng nhất.
  (b) Trang `admin/(protected)/page.tsx` (Tổng quan) hiện chỉ là thẻ điều hướng, KHÔNG có số liệu tiền → không cần guard riêng. Thêm comment cảnh báo: nếu sau này thêm KPI doanh thu vào Tổng quan thì phải bọc bằng `canManage(role,'reports')` — nếu không sẽ rò rỉ đúng thứ AUTH-05 vừa chặn. Đồng thời các thẻ điều hướng trên trang này phải ẩn theo cùng quy tắc nav (thẻ "Cài đặt" không hiện với manager).
  (c) KHÔNG đụng RLS/migration (QD-010 §Hệ quả: V1 chấp nhận RBAC ở tầng ứng dụng).
  </action>
  <verify>Đăng nhập manager: sidebar không có "Cài đặt"; thẻ "Cài đặt" trên Tổng quan không hiện; gõ /admin/settings → về /admin; POST thẳng vào updateSettings → "Không đủ quyền"</verify>
  <acceptance_criteria>
    - Manager bị chặn settings ở CẢ page và server action (không chỉ ẩn nav)
    - Mọi trang admin còn lại có guard cấp trang đúng section
  </acceptance_criteria>
  <done>AUTH-05 phần guard</done>
</task>

<task type="auto">
  <name>Task 3: Owner tạo được tài khoản manager (mật khẩu mạnh)</name>
  <files>app/r/[slug]/admin/(protected)/staff/actions.ts, app/r/[slug]/admin/(protected)/staff/page.tsx</files>
  <read_first>app/r/[slug]/admin/(protected)/staff/actions.ts (toàn bộ: PIN_ROLES, requireManager, createStaff, resetPin, setStaffActive, deleteStaff), staff/page.tsx (form), lib/auth/staff-credentials.ts (derivePinPassword), lib/auth/pin.ts (isValidPin, hashPin)</read_first>
  <action>
  actions.ts — `createStaff`:
  (a) Bỏ hằng `PIN_ROLES` làm điều kiện chặn duy nhất; thay bằng `canAssignRole(session.role, role)` (Task 1e). Trả lỗi "Không đủ quyền cấp vai trò này." nếu false. **Guard này nằm ở SERVER** — ẩn option trong form là trang trí, không phải bảo mật.
  (b) Tách 2 nhánh cấp bí mật:
      - role ∈ {cashier, waiter, kitchen} → như cũ: PIN 4 số, `derivePinPassword(email, pin)`, `pin_hash = hashPin(pin)`.
      - role = manager → đọc field `password`, yêu cầu **≥8 ký tự** và KHÔNG phải chuỗi đúng 4 chữ số (tránh đụng quy tắc suy dẫn PIN của QD-009 §Hệ quả). `createUser({password})` thẳng, `pin_hash = null`.
  (c) `resetPin` cũng tách nhánh tương ứng — với manager là "đặt lại mật khẩu" (≥8 ký tự), không phải PIN.
  (d) `deleteStaff` / `setStaffActive`: thêm chặn — không cho tác động lên membership có `role='owner'`, và manager không tác động được lên membership `role='manager'` (dùng `canAssignRole(session.role, target.role)`; phải ĐỌC vai trò của target trước, đừng tin `formData`).

  staff/page.tsx: select vai trò hiện thêm "Quản lý" **chỉ khi** `canAssignRole(session.role,'manager')`. Chọn "Quản lý" → ô nhập đổi từ "PIN 4 số" sang "Mật khẩu (≥8 ký tự)". Danh sách nhân viên hiện Badge vai trò cho manager; hàng của owner hiện nhưng không có nút Xóa/Đặt lại.
  </action>
  <verify>Owner tạo được manager (mật khẩu ≥8) → đăng nhập /admin/login vào được admin, không thấy Cài đặt; manager mở /admin/staff KHÔNG thấy option "Quản lý" và POST tay role=manager bị từ chối; không ai xóa được owner</verify>
  <acceptance_criteria>
    - Owner tạo/đặt lại được tài khoản manager bằng mật khẩu ≥8 ký tự (không PIN)
    - Manager không tạo/sửa/xóa được manager hay owner — chặn ở SERVER
    - Nhánh cashier/waiter/kitchen (PIN 4 số) KHÔNG đổi hành vi
  </acceptance_criteria>
  <done>AUTH-06</done>
</task>

<task type="auto">
  <name>Task 4: "Hết món" ở POS + KDS</name>
  <files>app/r/[slug]/admin/(protected)/menu/actions.ts, components/pos/MenuPanel.tsx, components/kds/SoldOutDrawer.tsx, components/kds/KdsBoard.tsx, app/r/[slug]/kds/page.tsx</files>
  <read_first>app/r/[slug]/admin/(protected)/menu/actions.ts (requireManage + setItemAvailable dòng 339), components/menu/AvailabilityToggle.tsx, components/pos/MenuPanel.tsx (dòng 39 tap + dòng 52 filter), lib/orders/customer-menu.ts (getCustomerMenu), app/r/[slug]/pos/page.tsx (nguồn menu), components/kds/KdsBoard.tsx, app/r/[slug]/kds/page.tsx</read_first>
  <action>
  (a) menu/actions.ts: `setItemAvailable` đổi guard riêng sang `canToggleAvailability(session.role)` (các action còn lại GIỮ `canManage(role,'menu')` — đây là chỗ dễ nới tay quá đà nhất của plan này). Action chỉ `update({ is_available })` theo `id` + `tenant_id`; tuyệt đối không nhận thêm field nào khác từ client. `revalidatePath` cả `/r/{slug}/pos`, `/r/{slug}/kds` và `/r/{slug}/menu` ngoài đường dẫn admin.
  (b) MenuPanel.tsx: BỎ lọc `i.is_available` ở dòng 52 (getCustomerMenu vốn đã trả cả món hết) → món hết vẫn hiện nhưng làm mờ + nhãn "Hết", `tap()` giữ nguyên chặn ở dòng 39 (không thêm vào giỏ được). Mỗi thẻ món gắn `<AvailabilityToggle>` ở góc; toggle **không** được kích hoạt `tap()` (chặn nổi bọt sự kiện — đây là lỗi dễ mắc: bấm "Hết" lại mở ModifierSheet).
  (c) components/kds/SoldOutDrawer.tsx (mới, client): drawer "Báo hết món" — ô tìm không dấu (dùng lại hàm `norm` của MenuPanel, tách ra dùng chung) + danh sách món phẳng + `AvailabilityToggle`. Bếp là người biết hết món đầu tiên, mà vai trò `kitchen` KHÔNG vào được `/pos` (`canAccess`) — nên không có drawer này thì MENU-04 hụt đúng người cần nhất.
  (d) kds/page.tsx: gọi `getCustomerMenu(slug)` truyền vào `KdsBoard`; KdsBoard thêm nút mở SoldOutDrawer ở header. Nút ≥44px (OPS-05, bề mặt dày đặc).
  </action>
  <verify>Đăng nhập cashier ở /pos → bấm "Hết" trên 1 món → thẻ mờ + nhãn Hết, không thêm vào giỏ được; khách tải lại /menu thấy món đó "Hết". Đăng nhập kitchen ở /kds → drawer "Báo hết món" → tắt/bật được. Cashier POST tay vào updateItem → từ chối.</verify>
  <acceptance_criteria>
    - Nhân viên POS và bếp KDS bật/tắt "hết món" mà không vào /admin
    - Bấm toggle không mở ModifierSheet
    - Quyền nới đúng 1 cột: setItemAvailable mở, updateItem/deleteItem vẫn chặn
  </acceptance_criteria>
  <done>MENU-04</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phân quyền chi tiết khu admin (QD-010): ma trận canManage, sidebar theo quyền, owner tạo được manager bằng mật khẩu mạnh, "hết món" chuyển xuống POS/KDS. Dev localhost:3000, tenant `pho-viet`.</what-built>
  <how-to-verify>
    1. (Owner) `/admin/staff` → tạo "Quản lý" tên Trang, email `trang@pho-viet.test`, mật khẩu `TrangPass123!` → tạo được.
    2. (Owner) sidebar đủ 6 mục; `/admin/settings` mở bình thường, sửa %VAT được.
    3. Đăng xuất → đăng nhập `trang@...` ở `/admin/login` → vào được `/admin`.
    4. (Manager) sidebar **KHÔNG có "Cài đặt"**; trang Tổng quan không có thẻ "Cài đặt".
    5. (Manager) gõ thẳng `/r/pho-viet/admin/settings` → **bị đá về `/admin`**, không kịp thấy nội dung.
    6. (Manager) `/admin/reports` → **xem được** doanh thu (đúng thiết kế — quản lý chốt ca).
    7. (Manager) `/admin/staff` → select vai trò **không có** "Quản lý"; xóa/đặt lại trên hàng của owner không khả dụng.
    8. (Manager) `/admin/menu` → sửa món, đổi giá bình thường.
    9. Đăng nhập cashier (`lan@pho-viet.test` + PIN) ở `/pos/login` → `/pos` → bấm "Hết" trên Phở bò → thẻ mờ + nhãn "Hết"; chạm vào **không** mở sheet thêm món.
    10. Mở `/r/pho-viet/menu` bằng tab ẩn danh → Phở bò hiện "Hết", không đặt được.
    11. Đăng nhập kitchen (`hung@pho-viet.test` + PIN) ở `/kds/login` → nút "Báo hết món" → tìm "pho" → bật Phở bò về "Còn" → tab khách tải lại thấy đặt được.
    12. (Cashier) gõ `/r/pho-viet/admin` → vẫn bị đá về `/pos` (ngưỡng vào admin KHÔNG đổi).
    13. (Owner Bún Bò) không thấy nhân viên/món của Phở Việt (RLS không hồi quy).
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (manager vẫn vào được settings, nav ẩn nhầm mục, toggle mở nhầm sheet, tạo manager lỗi Supabase).</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/auth/rbac.test.ts` xanh (6 vai trò × 6 mục + canToggleAvailability + canAssignRole)
- [ ] `npx tsc --noEmit` + `npm run lint` sạch
- [ ] Manager bị chặn settings ở page **và** server action; nav ẩn đúng mục (AUTH-05)
- [ ] Owner tạo được manager bằng mật khẩu ≥8; manager không nhân bản được quyền — chặn ở server (AUTH-06)
- [ ] Cashier + kitchen bật/tắt "hết món" ngoài admin; updateItem vẫn từ chối (MENU-04)
- [ ] `npm run test:rls` vẫn 6/6 PASS (không hồi quy cách ly tenant)
</verification>

<success_criteria>
- Checkpoint "approved"
- AUTH-05, AUTH-06, MENU-04 đóng; AUTH-04 nâng từ "phân quyền theo bề mặt" lên "phân quyền theo mục trong bề mặt"
</success_criteria>

<output>
Tạo `docs/30-KeHoach/P6/06-01-SUMMARY.md` (file đã đổi, trạng thái từng cam kết, bằng chứng).
Cập nhật `docs/00-TongQuan/BanDoLienKet.md` §3 khu Admin: `/admin/settings` → **owner**; các route còn lại giữ owner/manager. Bỏ dòng `/admin/data-scope` (route không còn tồn tại trong `app/`).
Cập nhật `docs/00-TongQuan/Roadmap.md` (P6 — tick mục phân quyền) và `docs/20-DanhSachYeuCau/00-Requirements.md` (AUTH-05/06, MENU-04 → ☑ kèm bằng chứng).
</output>
